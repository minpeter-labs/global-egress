# Bind to loopback by default. A Tailscale address is suitable when OpenCode runs
# on another machine in the same tailnet.
ZEN_PUBLIC_LISTEN=127.0.0.1:8090

# One attempt per distinct measured public IP is useful. Attempts above the
# available unique-IP inventory stop naturally when the pool is exhausted.
ZEN_PUBLIC_ATTEMPTS=8
